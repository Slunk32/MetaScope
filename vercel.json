{
    "version": 2,
    "builds": [
        {
            "src": "backend/package.json",
            "use": "@vercel/next"
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/backend/api/$1"
        }
    ]
}