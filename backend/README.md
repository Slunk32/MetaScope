# MetaScope Backend

This is the proxy server for MetaScope.

## Stack
- Next.js (App Router)
- Vercel (Hosting)

## API Routes
- `GET /api/events`: Scrapes MTGO decklists and returns JSON.

## Development
```bash
npm run dev
```
Runs on `http://localhost:3000`.
